flow:
  id: 'get-customer'
  description: 'Get customer by id flow'
  ttl: 10s

first.task: 'customer.get'

tasks:
  - input:
      - 'input.path_parameter.id -> id'
    process: 'customer.get'
    output:
      - 'result -> output.body'
    description: 'Get customer by id'
    execution: end