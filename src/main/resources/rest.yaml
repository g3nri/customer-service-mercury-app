rest:
  - service: "http.flow.adapter"
    methods: [POST]
    url: "/api/customers"
    flow: "create-customer"
    timeout: 10s
    tracing: true

  - service: "http.flow.adapter"
    methods: [GET]
    url: "/api/customers/{id}"
    flow: "get-customer"
    timeout: 10s
    tracing: true